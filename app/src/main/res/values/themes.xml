<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DogSitterFinder" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/theme_color</item>
    </style>

    <style name="Theme.DogSitterFinder" parent="Base.Theme.DogSitterFinder" />

    <style name="Theme.DogSitterFinder.Fullscreen" parent="Theme.DogSitterFinder">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.DogSitterFinder.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.DogSitterFinder.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>